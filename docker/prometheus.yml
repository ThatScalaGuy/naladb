global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "naladb"
    static_configs:
      - targets:
          - "naladb-0:9090"
          - "naladb-1:9090"
          - "naladb-2:9090"
        labels:
          cluster: "naladb-docker"
